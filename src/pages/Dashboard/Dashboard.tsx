import React, { useEffect } from 'react'
import {User} from '../../utils/localStorage'
// create interface of array of objects


const Dashboard = () => {

  return (
    <div>Dashboard</div>
  )
}

export default Dashboard